function panaderiaTulipan(compraunpan) {
    // Elabora un algoritmo que determine tu decision para comprar un pan y un cafe
    // Devolver el enunciado correcto
    // Escribe tu código aquí:
    var cafecito = "cafe con leche";
    var panecillo = "pan de dulce";
    var statement = "estoy bien";
        if(statement === true) {
            return "entra y compra un " + panecillo + " con un " + cafecito;
        } else {
            return "entra y compra un " + cafecito + " con un " + panecillo;
        }
}